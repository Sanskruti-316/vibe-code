import { GalleryHeader } from "../GalleryHeader";
import { ThemeProvider } from "../ThemeProvider";

export default function GalleryHeaderExample() {
  return (
    <ThemeProvider>
      <GalleryHeader />
    </ThemeProvider>
  );
}
